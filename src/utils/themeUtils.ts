// Theme utilities - cleaned up for new CSS variable-based theming system
// Previous theme merging functions removed as they're no longer needed
// with the new WidgemoThemeProvider + CSS variable approach